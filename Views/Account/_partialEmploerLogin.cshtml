@model NewLetter.Models.login
@using (Html.BeginForm("_Employerlogin", "Account", null, FormMethod.Post, new { @class = "login-form" }))
{   

    @Html.ValidationMessageFor(m => m.qenEmail, "", new { @class = "text-danger" })
    <div class="input-group">
        <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
        @*<input id="login-username" type="text" class="form-control" name="username" value="" placeholder="Username">*@
        @Html.TextBoxFor(m => m.qenEmail, new { @class = "form-control", placeholder = "Email" })

    </div>
    @Html.ValidationMessageFor(m => m.password, "", new { @class = "text-danger" })
    <div class="input-group">
        <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
        @*<input id="login-password" type="password" class="form-control" name="password" placeholder="password">*@
        @Html.PasswordFor(m => m.password, new { @class = "form-control", placeholder = "password" })

    </div>
    <div class="col-md-4 col-xs-4 col-xs-4 padding-none">
        <button type="submit" class="btn">Login</button>
    </div>
}
@*<div class="col-md-8 col-sm-8 col-xs-8 text-right padding-none"> <a href="/Account/eforgotPassword" class="forgot-password">Forgot Password?</a> </div>*@
<div class="col-md-8 col-sm-8 col-xs-8 text-right padding-none"> <a href="#" class="forgot-password" data-toggle="modal" data-target="#eForgotPassModal">Forgot Password?</a> </div>
<div class="modal fade" id="eForgotPassModal" role="dialog">
    <div class="modal-dialog">
        @Html.Action("_partailEforgotPassword")
        </div>
    </div>
